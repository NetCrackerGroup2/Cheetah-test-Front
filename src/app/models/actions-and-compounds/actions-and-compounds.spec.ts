import { ActionsAndCompounds } from './actions-and-compounds';

describe('ActionsAndCompounds', () => {
  it('should create an instance', () => {
    expect(new ActionsAndCompounds()).toBeTruthy();
  });
});
