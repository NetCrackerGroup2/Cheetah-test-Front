import { CompoundDtoWithActions } from './compound-dto-with-actions';

describe('CreateCompoundDto', () => {
  it('should create an instance', () => {
    expect(new CompoundDtoWithActions()).toBeTruthy();
  });
});
